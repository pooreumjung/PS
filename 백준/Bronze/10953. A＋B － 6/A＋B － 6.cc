#include<iostream>
#include<queue>
using namespace std;
int main() {
	int n;
	cin >> n;
	for (int x = 0; x < n; x++) {
		int a, b;
		char c;
		cin >> a >> c >> b;
		cout << a + b << endl;
	}
}